# Decision log

## 2026-07-16 — 월별 행동 루프를 운영 리비전과 외부 헬스체크로 검증

Decision: `gpt-5.6-luna`, 30초 Cloud Run 제한, 애플리케이션 health endpoint와 보안 헤더를 포함한 revision `wealth-copy-00005-crx`를 서울 리전에 배포하고 100% 트래픽을 연결한다. Cloud Run의 지역 별칭과 정식 서비스 URL이 다르더라도 브라우저가 생성한 `Sec-Fetch-Site: same-origin` 요청은 허용하되 `cross-site`와 외부 Origin은 계속 차단한다. Cloud Monitoring에는 공개 `/api/healthz`를 5분마다 확인하는 uptime check를 둔다.

Why: 서버 간 API 점검만으로는 사용자가 실제 브라우저에서 계획을 만들 수 있는지 보장할 수 없다. 또한 health endpoint만 있고 외부 감시가 없으면 서비스 중단을 지속적으로 발견할 운영 신호가 없다.

Evidence: 공개 지역 URL의 실제 3단계 입력으로 `L7 → L8` 행동 세 개를 생성하고 첫 행동 완료 시 진행률 `0% → 33%`와 접근성 상태 문구 갱신을 확인했다. 라이브 API에서 `L1 → L2`, `L14 → L15`, `L15 → L15`, 이전 달 이력 기반 운영 행동과 실제 모델 기반 안전 후보 선택을 확인했다. 비 JSON·외부 Origin·압축·8KB 초과 요청은 의도한 4xx와 no-store로 거절됐고 새 리비전의 오류 로그는 0건이었다.

Limits: uptime check는 장애를 측정하지만 아직 알림 채널이나 당직 체계에 연결되지 않았다. 인증, 분산 rate limit, 예산 알림, 데이터 보존·삭제 정책과 금융·법률 검토는 정식 고객 트래픽 전에 필요하다.

## 2026-07-16 — 완료 이력을 다음 달 운영 행동으로 연결

Decision: 계획 저장과 분리된 `wealthcopy-action-history-v1`에 최대 36개의 `행동 ID·출발 레벨·완료 월`만 저장한다. 서버에는 현재 달을 제외한 1–11개월 전 최소 완료 신호만 보내며, 서버가 새로 분류한 출발 레벨과 같은 이력만 사용한다. 기반 보호는 미완료 우선, 전환 앵커는 이전 완료 시 레벨군별 실제 운영 행동, 결과 확인은 미완료 후 가장 오래된 항목 순으로 회전한다. 현재 위험의 hard stop은 이력으로 억제하지 않는다.

Why: 기존 월 변경은 계획과 완료 맥락을 모두 버려 같은 규칙 만들기를 반복했다. 첫 달 설정을 다음 달 실제 실행과 점검으로 연결해야 사용자가 재방문할 이유와 누적 효용이 생긴다.

Evidence: 이력 스키마는 금액·비율·PSID·메모·세션을 거절하고 12개월·36건 한도를 검증한다. 전 레벨 테스트는 `protect → advance → verify` 순서와 단계군별 운영 행동을 검증하고, 악성·타 레벨 이력과 현재 달 이력이 레벨이나 모델 입력을 바꿀 수 없음을 확인한다.

Limits: 완료 이력은 행동 중복 방지를 위한 로컬 편의 데이터다. 자산 변화, 성과, 순자산 단계 도달 또는 조언 적합성의 증거가 아니다.

## 2026-07-16 — 공개 API와 모델 선택 경계를 운영 수준으로 강화

Decision: 계획 API는 `application/json`, same-origin, identity content encoding만 받고 `Content-Length` 조기 차단과 스트리밍 8KB 한도를 모두 적용한다. 모든 화면에 보안 헤더를 적용하고 API와 루트 HTML은 재검증·no-store 경계를 사용한다. 제한된 단일 enum 선택에는 Structured Outputs를 지원하는 `gpt-5.6-luna`를 low reasoning, 10초 SDK timeout, 재시도 없음으로 사용하며 실패 시 규칙 fallback을 반환한다.

Why: 브라우저의 단순 요청 우회, 큰 본문 선적재, 긴 모델 재시도는 공개 데모의 남용·지연·비용 위험을 키운다. 정적 후보 하나를 고르는 작업에는 고성능 장문 모델 경로보다 빠르고 제한된 선택 경계가 적합하다.

Evidence: 잘못된 content type·cross-site·foreign origin·압축 본문·초과 스트림이 모델 호출 전에 거절되고, 정상 same-origin 요청과 health endpoint가 별도 테스트된다. PostCSS는 취약 버전을 override했고 production audit은 알려진 취약점 0건이다.

Limits: IP·세션 rate limit은 여전히 인스턴스 메모리 기반이다. 정식 고객 서비스에는 인증, 분산 제한, 동의·삭제·보존 정책, uptime/오류/비용 알림과 금융·법률 검토가 추가로 필요하다.

## 2026-07-16 — 행동을 목적 경로와 protect·advance·verify로 재구성

Decision: `behavior-policy-v2`는 현금 방어, 부채 통제, 소득 회복력, 코어 구축, 쏠림 통제, 유동성 계획, 자산 운영 체계, 연속성의 여덟 목적 경로 중 현재 병목 하나를 내부 선택한다. 공개 행동은 항상 `protect → advance → verify` 순서다. 첫 행동은 안전 신호를 반영하는 기반 보호 행동, 두 번째는 레벨별 전환 또는 운영 앵커, 세 번째는 결과를 확인하는 증거 행동이다. hard stop이 없을 때만 모델이 안전 후보 중 하나의 `supportActionId`를 선택한다.

Why: 행동이 막연한 검토나 공통 일정이면 사용자가 무엇을 만들고 언제 완료인지 판단할 수 없다. 보호, 전진, 검증을 분리하고 단계별 앵커를 고정하면 자산 단계와 현재 생활 제약이 모두 반영되면서도 모델의 역할을 좁게 유지할 수 있다.

Evidence: 모든 공개 행동은 정적 `남는 결과`, 이진 `완료 기준`, 정확히 세 개의 `실행 순서`를 가진다. 전 레벨 테스트는 세 단계 순서와 고정 앵커를 검증하고, 모델 경계 테스트는 허용되지 않은 ID·PSID·정확한 금액·레벨·원 비율이 모델 선택을 바꿀 수 없음을 확인한다.

Limits: 목적 경로와 행동은 제품 내부 교육용 정리 정책이다. 위험성향, 적합성, 기대수익, 투자 배분, 상품 추천 또는 다음 레벨 도달 속도를 뜻하지 않는다.

## 2026-07-16 — 메인 화면을 행동 중심 프라이빗 웰스 콘솔로 단순화

Decision: 초기 화면의 가짜 `NEXT`, `0%`, 비활성 행동 카드를 제거하고 계획이 생성된 뒤에만 `다음 자산 단계`, `행동 3개`, `행동 완료 진행률`을 노출한다. 활성 계획은 3열 카드 대신 하나의 세로 실행 원장으로 표시하고, 자산정보 입력은 `가구 자산 → 자산 구조 → 실행 여건`의 실제 3단계 흐름으로 분리한다. 각 행동에서는 남는 결과와 완료 기준을 즉시 보여 주고 세 단계 실행 순서와 체크리스트 복사를 선택적으로 펼친다. 시각 언어는 따뜻한 페이퍼 배경, 미네랄 그린, 헤어라인과 제한된 그림자를 사용하는 Quiet Private Banking 방향으로 통일한다.

Why: 분석 정보와 비어 있는 대시보드를 먼저 보여주면 사용자가 판단해야 할 항목이 늘고 서비스가 미완성처럼 보인다. 다음 한 단계와 이번 달 행동만 선명하게 보여줘야 WealthCopy의 정체성인 `복제 → 실행 → 완료`가 즉시 이해된다.

Evidence: 320px, 390px, 1440px 뷰포트에서 가로 넘침 없이 초기·입력·활성·완료 화면을 검증했다. 실제 계획 API로 `L6 → L7`과 `L15 → L15` 유지 경로를 생성하고 행동 완료율 `0 → 33 → 67 → 100%` 연결을 확인했다. 전체 60개 테스트, lint, typecheck, production build가 통과했으며 Cloud Run revision `wealth-copy-00002-jmh`가 100% 트래픽을 받고 배포 후 오류 로그는 0건이다.

Limits: 고급화된 화면도 교육용 행동 기록 도구이며 금융 거래, 투자 판단, 목표 단계 도달을 보장하지 않는다. 행동 3개 완료 후에도 최신 자산 스냅샷으로 레벨을 다시 분류해야 한다.

## 2026-07-15 — 서울 Cloud Run에 최소권한 공개 데모 배포

Decision: Next.js를 Node.js 24 standalone 컨테이너로 만들고 GCP 프로젝트 `abis-web-platform`의 서울 리전 Cloud Run 서비스 `wealth-copy`에 배포한다. 런타임은 전용 서비스 계정 `wealth-copy-run`을 사용하고 OpenAI 키는 Secret Manager의 버전 고정 환경변수로만 주입한다. 공개 데모는 최소 인스턴스 0, 최대 3, 동시 요청 20, 요청 제한 120초로 운영한다.

Why: 메인 화면과 서버 계획 API를 같은 origin에서 운영하면서 TLS, scale-to-zero, revision과 로그를 관리형 서비스로 확보할 수 있다. 전용 서비스 계정과 secret 단위 접근 권한은 다른 프로젝트 리소스와 OpenAI 키의 권한 범위를 줄인다.

Evidence: revision `wealth-copy-00001-lkh`가 100% 트래픽을 받고 있으며 공개 페이지, 실제 OpenAI 요청, `L1 → L2`, `L14 → L15`, `L15 → L15` API 계약과 no-store 헤더가 검증됐다. 배포 후 Cloud Run 오류 로그와 OpenAI 오류 로그는 0건이었다.

Limits: 현재 IP·세션 rate limit은 인스턴스 메모리에만 있어 scale-out 전체에 대한 전역 제한이 아니다. 정식 운영 전 인증, 분산 rate limit, 비용 알림, 보존·삭제 정책, 모니터링과 법률 검토가 필요하다. Cloud Run 기본 TCP startup probe를 사용하며 별도 애플리케이션 health endpoint는 아직 없다.

## 2026-07-15 — 가구 순자산으로 L1–L15를 자동분류

Decision: 사용자가 입력한 `가구 총자산`과 `가구 총부채`로 `가구 순자산 = 총자산 - 총부채`를 계산하고 `krw-net-worth-v1` 제품 정책에 따라 현재 레벨을 자동분류한다. 클라이언트가 현재 레벨을 직접 지정하지 않는다.

| Level | 가구 순자산 |
| --- | ---: |
| `L1` | 0원 미만 |
| `L2` | 0원 이상 1천만원 미만 |
| `L3` | 1천만원 이상 3천만원 미만 |
| `L4` | 3천만원 이상 1억원 미만 |
| `L5` | 1억원 이상 3억원 미만 |
| `L6` | 3억원 이상 5억원 미만 |
| `L7` | 5억원 이상 10억원 미만 |
| `L8` | 10억원 이상 30억원 미만 |
| `L9` | 30억원 이상 50억원 미만 |
| `L10` | 50억원 이상 100억원 미만 |
| `L11` | 100억원 이상 300억원 미만 |
| `L12` | 300억원 이상 1,000억원 미만 |
| `L13` | 1,000억원 이상 3,000억원 미만 |
| `L14` | 3,000억원 이상 1조원 미만 |
| `L15` | 1조원 이상 |

Why: 자가선택 단계는 사용자마다 해석이 달라 경로의 출발점이 일관되지 않았다. 하나의 버전형 순자산 정책으로 분류해야 모든 사용자가 동일한 경계와 다음 단계 규칙을 사용한다.

Evidence: 정책 모듈은 총자산·총부채를 0 이상의 safe integer로 제한하고, 모든 정확한 하한과 하한 직전 값, 음의 순자산, 부채 차감 사례를 테스트한다. 모든 하한은 포함하고 상한은 제외한다.

Limits: 이 레벨은 WealthCopy가 행동을 구성하기 위한 제품 구간이다. 공식 자산등급, 한국 자산 백분위, 신용등급, 투자 적합성 또는 미래 성과가 아니다. 경계 변경에는 새 정책 버전과 저장 마이그레이션이 필요하다.

## 2026-07-15 — L15는 최상위 유지 단계

Decision: `L1 → L2`부터 `L14 → L15`까지 한 단계씩 연결하고, `L15`는 더 높은 목표를 만들지 않고 `L15 → L15` 유지·운영 점검 행동을 제공한다.

Why: 1조원 이상 구간에서 존재하지 않는 다음 등급을 제시하면 제품 계약과 사용자 카피가 거짓이 된다. 유지 단계도 매월 기록·권한·연속성을 점검할 실질적 행동이 필요하다.

Limits: L15 행동은 수익 확대, 투자 배분, 세금·법률 결론 또는 거래를 제안하지 않는다.

## 2026-07-15 — 정확한 금융 금액을 분류 경계에 격리

Decision: 정확한 총자산·총부채는 요청의 비공개 서버 경계에서 현재 레벨과 거친 레버리지 밴드를 계산하는 동안만 사용한다. 금액을 OpenAI 모델 입력, 성공 응답, 브라우저 저장소, 분석 이벤트 또는 애플리케이션 로그에 포함하지 않는다. API 응답은 `Cache-Control: no-store`, 모델 호출은 `store: false`를 사용한다.

Why: 다음 행동을 만들기 위해 필요한 것은 분류된 단계와 통화 중립 행동 신호이지 정확한 가구 금액이 아니다. 부채/자산 관계도 원 금액 대신 `none·low·medium·high·underwater` 밴드로만 전달해 민감정보의 이동과 보존을 최소화한다.

Evidence: 수집 프로필은 서버 경계에서 분류와 밴드 생성이 끝난 뒤 금액을 제거한다. 모델 입력과 공개 계획은 각각 strict 스키마로 다시 투영되며 저장 레코드는 공개 계획과 출발 레벨만 받는다.

Limits: 계산된 다음 단계와 행동 완료 기록은 기기 내 저장 대상이므로 정확한 금액이 없더라도 금융 맥락을 드러낼 수 있다. 사용자에게 이를 고지하고 삭제 기능을 제공한다.

## 2026-07-15 — PSID 참고값과 원화 레벨 정책을 분리

Decision: `psid-wealth-reference-v2`는 공개 PSID 보고서 `Table 4`의 `PSID 2019` 열에 있는 미국 가족 순자산 집계와 출처 메타데이터를 감사용으로 고정하고, 사용자에게는 백분위 경계 구조만 선택형 참고로 제공한다. `krw-net-worth-v1`의 L1–L15 분류, `behavior-policy-v2`의 목적 경로, 공개 행동, OpenAI 모델 입력은 PSID 값을 사용하지 않는다.

Why: 미국 달러 순자산 분포를 한국 원화 경계로 환산하거나 개인의 한국 백분위로 해석하면 거짓 정밀도가 생긴다. 관측 자료, 제품 분류, 행동 정책을 분리해야 한다.

Evidence: PSID 구간을 바꾸거나 `unknown`으로 두어도 현재 레벨, 여덟 경로의 점수와 승자, 행동 세 개가 달라지지 않는다. 모델 입력에는 PSID 선택값과 달러 경계값이 모두 없다.

Limits: 사용자가 고른 참고 구간은 실제 백분위, 공식 등급, 투자 성향, 위험 감수 능력 또는 단계 도달 가능성을 의미하지 않는다. Public Use 마이크로데이터, Restricted Data와 개별 가족 기록은 사용하지 않는다.

## 2026-07-15 — 3/3 완료와 자산 단계 도달을 분리

Decision: `3/3 · 100%`는 이번 달 세 행동을 모두 체크했다는 뜻만 가진다. 행동 완료로 현재 레벨을 변경하거나 과거 `nextLevel`을 새 현재 레벨로 승계하지 않는다. 새 달에는 항상 최신 자산 스냅샷으로 재분류한다.

Why: 체크리스트 완료는 순자산 임계값 통과의 증거가 아니다. 특히 상위 구간에서 월간 행동과 자산 경계 사이에 인과관계를 주장할 근거가 없다.

Evidence: 완료 화면과 라이브 상태 메시지는 자동 승급을 부정하고 최신 자산정보 재입력을 안내한다. 진행률은 완료 행동 수에서만 결정된다.

Limits: 같은 달에 동일한 출발 단계와 다음 단계로 다시 생성된 계획만 동일 action ID의 완료 상태를 이어받을 수 있다.

## 2026-07-15 — 행동 정책 의미 변경에 따라 저장소를 v5로 교체

Decision: 브라우저 키를 `wealthcopy-public-plan-v5`로 올리고 정확히 `{version, monthKey, sourceLevel, plan}`만 저장하며 `version`은 `5`로 고정한다. 익명 session UUID는 금융정보 없이 `wealthcopy-anonymous-session`이라는 별도 localStorage 키에 둔다. 과거 `wealthcopy-public-plan-v4`, `wealthcopy-public-plan-v3`, `wealthcopy-public-plan-v2`, `wealthcopy-demo-plan-v1`은 새 행동이나 완료 상태로 변환하지 않고 제거한다.

Why: v2 행동 ID와 `protect → advance → verify` 의미는 과거 공통 행동 기록과 호환되지 않는다. 문자열이 우연히 같더라도 자동 마이그레이션하면 검증되지 않은 행동 완료가 된다.

Evidence: 이전 키가 발견되거나 달이 바뀌면 저장 계획을 제거하고 최신 3단계 가구 스냅샷으로 다시 시작하라는 메시지를 표시한다. 같은 달에는 출발·다음 레벨이 모두 같고 동일한 행동 ID만 완료 상태를 유지할 수 있다.

## 2026-07-15 — 기반 보호·단계 전환·결과 확인으로 세 행동 구성

Decision: 모든 L1–L15 출발 단계는 하나의 검토된 전환 앵커와 하나 이상의 증거 행동을 가진다. 서버는 안전 제약으로 기반 보호 행동을 강제할 수 있고, hard stop이 없을 때 GPT‑5.6은 해당 목적 경로와 레벨의 허용 후보에서 하나의 `supportActionId`만 고른다. 전환 앵커와 결과 확인 행동은 모델과 무관하게 고정한다.

Why: 단계별 앵커가 실제 경로 차이를 만들고, 제한된 모델 선택은 현재 병목을 반영하며, 결과 확인 행동은 체크가 실제 산출물과 검증으로 이어지게 한다. 다음 달 연결은 특정 행동이 아니라 최신 스냅샷 재분류 규칙이 담당한다.

Limits: 모델은 사용자 카피, 금액, 순자산, 현재·다음 레벨, 수익률, 상품, 거래 또는 진행률을 만들거나 볼 수 없다. 모델이 없거나 출력이 유효하지 않아도 같은 공개 형태의 규칙 fallback을 반환한다.

## 2026-07-15 — 상위 레벨은 운영·거버넌스 행동으로 현실화

Decision: 상위 레벨 행동은 평가 기준일, 부채·보증 기록, 소유·관리 구조, 보고 일정, 의사결정 권한, 업무 연속성과 접근 절차를 점검한다.

Why: 높은 순자산 구간에서 단순한 저축 확대 행동이나 투자 상품 추천은 현실성이 낮고 금융 자문 위험이 높다. 기록과 운영 통제는 금액·수익률 없이도 완료 기준을 만들 수 있다.

Limits: 비밀번호, 인증수단, 계좌정보 또는 비밀정보를 기록·공유하도록 안내하지 않는다. 행동 체크는 자산 평가, 권한 부여, 소유권 변경 또는 거래 실행이 아니다.

## 2026-07-14 — 자산관리를 행동 문제로 정의

Decision: WealthCopy의 사용자 가치는 더 많은 분석이 아니라 이번 달 실행할 행동을 세 개로 줄이는 것이다.

Why: 정보와 비교가 늘어날수록 실행 부담도 커진다. 사용자가 다음 행동을 바로 확인하고 체크할 수 있어야 월간 습관으로 이어진다.

Alternatives: 경로 비교, 추천 이유, 금액·기간·수익 대시보드를 전면에 두는 방식은 제외한다.

## 2026-07-14 — 엄격한 공개 API 투영

Decision: `POST /api/v2/plan` 성공 JSON 본문의 최상위는 정확히 `{nextLevel, actions, progress}`이며 action은 정확히 `{id, completed}`이다. actions는 중복 없는 허용 ID 세 개다. 서버가 분류한 현재 레벨은 본문이 아니라 `X-WealthCopy-Source-Level` 응답 헤더에만 담아 UI가 출발·다음 단계의 연속성을 검증하게 하며, 응답 전체에 `Cache-Control: no-store`를 적용한다.

Why: 내부 분류와 분석이 클라이언트 계약으로 새어 나오지 않고 사용자 경험의 단순성이 데이터 경계에서도 유지된다.

Evidence: 현재 레벨, 금액, PSID 값, 경로, 모델, 출처, 점수와 설명을 성공 JSON 본문에서 제외하고 최종 결과를 strict Zod 스키마로 검증한다. 현재 레벨은 검증용 응답 헤더에만 존재한다.

## 2026-07-14 — Structured Outputs와 동일 형태 fallback

Decision: Responses API, `gpt-5.6`, Zod Structured Outputs와 의미 검증을 사용한다. 키 누락, API 오류, 검증 실패와 rate limit 상황에서도 fallback은 같은 공개 계약을 반환한다.

Why: AI 상태와 무관하게 행동 화면이 동일하며 사용자는 내부 공급자 상태를 이해할 필요가 없다.

## 2026-07-14 — 입력·남용·금융 안전 경계

Decision: 개인정보 형태와 금융 실행·소득 중단 조건을 모델 전에 차단하고, 8KB 본문 제한과 IP·세션 인메모리 rate limit을 적용한다. 어떤 행동도 실제 거래를 실행하지 않는다.

Follow-up: 공개 출시 전 인증 기반 분산 rate limiting, 모니터링, 명시적 동의·삭제 UX, 개인정보 보존 정책, 접근성 및 금융 법률 검토가 필요하다.
